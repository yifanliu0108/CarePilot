# Optional — only when the UI and API are on different hosts (e.g. two Railway services).
# No trailing slash. Omit for local dev (Vite proxies /api to the backend).
# VITE_API_BASE_URL=https://carepilot-backend-production-xxxx.up.railway.app
