# Railway config-as-code for the single-service deployment path.
# This makes the backend service build the SPA and then serve both UI + /api.

[build]
builder = "RAILPACK"
buildCommand = "npm run build:web"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
