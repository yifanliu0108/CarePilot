# Prefer Nixpacks/Railpack when there is no root Dockerfile.
# If you use Docker per service, set Dockerfile path:
#   carepilot-frontend → Dockerfile.frontend
#   carepilot-backend    → Dockerfile.backend

[build]
builder = "nixpacks"
