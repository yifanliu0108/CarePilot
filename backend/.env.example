# Copy this file to `.env` in this same folder (backend/) and paste your key on the GEMINI_API_KEY line.
# Get a key: https://aistudio.google.com/apikey
# Never commit `.env` — it stays on your machine only.

GEMINI_API_KEY=

# Optional — default is gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash

# Browser Use Cloud — https://cloud.browser-use.com/settings
# BROWSER_USE_API_KEY=

# PORT=3001
